<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Theme -->
    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/amethyst</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApp</item>
    </style>

    <!-- Main App Theme without ActionBar -->
    <style name="Theme.MyApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize colors here if needed -->
        <item name="android:statusBarColor">@color/amethyst</item>
        <item name="android:navigationBarColor">@color/amethyst</item>
    </style>

</resources>

